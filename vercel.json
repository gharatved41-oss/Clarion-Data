{
  "buildCommand": "cd insight-analyst-app && npm install && npm run build",
  "outputDirectory": "insight-analyst-app/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
